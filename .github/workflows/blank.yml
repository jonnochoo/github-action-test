name: Bump Version and Create PR

on:
  workflow_dispatch:
    inputs:
      versionIncrement:
        type: choice
        description: Version Increment
        options:
          - patch
          - minor
          - major
jobs:
  bump-version:
    runs-on: ubuntu-latest

    permissions:
      # Give the default GITHUB_TOKEN write permission to commit and push the
      # added or changed files to the repository.
      contents: write
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # The "Bump version" step updates Git so we need to setup the Git config first
      - name: Run
        run: |
          git config --local user.email "dev+${{ github.actor }}@irwinsolutions.com"
          git config --local user.name "${{ github.actor }}"          
                    
          npm version ${{ github.event.inputs.versionIncrement }} -m "[DEPLOY] %s" --git-tag-version false
          
          version=$(cat package.json | jq -r '.version')
          echo "Set version: $version"
          
          branchName="version/${{ github.event.inputs.versionIncrement }}"
          echo "Creating a branch: $branchName"          
          git branch "$branchName"
          
          git push -u origin "$branchName"

      - name: Create a pull request
        run: |
          version=cat package.json | jq -r '.version'
          gh pr create -B dev -H $branchName --title 'Deploy $version' --body 'Created by Github action'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
